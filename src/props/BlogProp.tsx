export interface BlogItem {
    title: string;
    subTitle: string;
    blog: string;
    uuid: string;
    createdAt: string;
    imageUrl: string;
}

export interface blogItem {
    blogItem: BlogItem;
}
